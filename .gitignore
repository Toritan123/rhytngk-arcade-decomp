# ── ROMs and binary game data — NEVER commit ──────────────────────────────────
roms/
mame_roms/
*.ic[0-9]*
*.rom
*.ipl
fpr-*
epr-*
sp50*
develop*
zukin*
317-*
315-*
main_eeprom*
x76f100*
dcnaodev*
aicadrv.bin
bios_aicadrv.bin

# ── Compressed ROM archives ────────────────────────────────────────────────────
*.zip

# ── Raw binary blobs (compiled objects, firmware dumps, etc.) ─────────────────
*.bin
*.o
*.obj

# ── AICA register capture log (large binary, session-specific) ────────────────
aica_writes.log
aica_channel_state.bin
aica_keyon_states.bin

# ── ic11 graphics asset extraction (~125 MB, ROM-derived) ─────────────────────
build/ic11/
build/ic11_farc_contents/

# ── SH-4 function-boundary table (regenerable via find_func_boundaries.py) ────
build/sh4_functions.json
build/sh4_functions_v2.json
build/sh4_functions_v3.json
build/sh4_callgraph_v3.json
build/farc_index_v3.json

# ── Captured WAV audio (copyrighted game audio, large files) ──────────────────
# Root-level captures from Flycast/MAME sessions:
rhytngk_*.wav
rhytngk_*.mid

# ── Flycast source tree (third-party; clone separately) ───────────────────────
flycast-src/

# ── MAME / Flycast runtime state ──────────────────────────────────────────────
nvram/
cfg/
snap/
~/Library/Application Support/Flycast/

# ── Build / CMake logs ────────────────────────────────────────────────────────
build.log
cmake.log
mame_stdout.log
mame_stderr.log
mame_err.log
mame.log
aica_stderr.log
error.log
ffmpeg.log
flycast_out.log
patched.log
patched_flycast.log

# ── Editor / OS junk ──────────────────────────────────────────────────────────
.DS_Store
*~
.vscode/
.idea/

# ── Python cache ──────────────────────────────────────────────────────────────
__pycache__/
*.pyc

# ── Claude session data ───────────────────────────────────────────────────────
.claude/


# ── Graphics assets extracted from ROMs (ROM-derived, big) ───────────────
extracted/
extracted_farc/
extracted_decompressed/
textures_png/
preview_textures/
